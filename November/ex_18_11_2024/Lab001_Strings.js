/*
Strings are the sequence of characters used to represent text

Create String => let str = "Suyash"
String length => str.length
String indices => str[0], str[1], str[2], str[3]

-> Strings are immutable in JavaScript. which can not be changed. 
*/

// Creating an String

let str = "Suyash"
console.log(str)
console.log(str.length)
console.log(str[3])